server:
  port: 8086

spring:
  application:
    name: ai-agent

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: INFO
    dev.reagentic: DEBUG
